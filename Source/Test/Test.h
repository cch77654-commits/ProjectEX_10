#pragma once

#include "CoreMinimal.h"

// ════════════════════════════════════════════════════════════════════
// Test 모듈의 "모듈 헤더" 파일입니다.
// 관례적으로 모듈 이름과 동일한 이름의 .h/.cpp 한 쌍이 모듈의 진입점
// (IModuleInterface 구현부) 역할을 합니다.
// 지금은 이 모듈에 별도의 전역 함수/매크로가 필요 없어서 CoreMinimal.h만
// include하고 있지만, 모듈 전역에서 공용으로 쓸 로그 카테고리나
// 유틸 함수가 생기면 이 헤더에 선언을 추가하면 됩니다.
// ════════════════════════════════════════════════════════════════════
